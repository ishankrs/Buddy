---
description: Debug errors with root cause and verification (mirrors Buddy /debug)
agent: build
---

Focus on the reported error. Reproduce or locate it, identify the root cause
(not just symptoms), fix it minimally, then verify with `npm run check` and
the relevant build/test command. Show before/after evidence. $ARGUMENTS
